import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import App from '../App'

describe('App', () => {
  it('renders without throwing', () => {
    // This is a basic test to ensure the testing setup works
    expect(true).toBe(true)
  })
})